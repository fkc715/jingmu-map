# Theatre Data

This directory is reserved for structured Beijing theatre seed data.

The first seed file will likely be a JSON file containing theatre names, districts, approximate coordinates, addresses, and display metadata.

No theatre dataset has been added yet.

